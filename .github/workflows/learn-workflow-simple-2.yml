name: 'simple workflow express'
on: [push]
jobs: 
  check-express-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - run: npm install -g express
      - run: express --version